# Config

This directory holds profile configuration for the candidate.

- `profile.example.yml` is the setup template.
- `profile.yml` is the active user-specific profile and should be treated as user data.

