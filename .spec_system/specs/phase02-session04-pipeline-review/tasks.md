# Task Checklist

**Session ID**: `phase02-session04-pipeline-review`
**Total Tasks**: 20
**Estimated Duration**: 2.5-3.5 hours
**Created**: 2026-04-23

---

## Legend

- `[x]` = Completed
- `[ ]` = Pending
- `[P]` = Parallelizable (can run with other [P] tasks)
- `[S0204]` = Session reference (02=phase number, 04=session number)
- `TNNN` = Task ID

---

## Progress Summary

| Category       | Total  | Done   | Remaining |
| -------------- | ------ | ------ | --------- |
| Setup          | 2      | 2      | 0         |
| Foundation     | 5      | 5      | 0         |
| Implementation | 9      | 9      | 0         |
| Testing        | 4      | 4      | 0         |
| **Total**      | **20** | **20** | **0**     |

---

## Setup (2 tasks)

Initial configuration and environment preparation.

- [x] T001 [S0204] Verify prerequisites met -- confirm TypeScript compiles, Vite builds, banned-terms check runs, and sessions 01-03 spec dirs exist (`apps/web/`)
- [x] T002 [S0204] Add pipeline-specific design tokens to tokens.css -- queue state tones (pending-bg/fg, processed-bg/fg, malformed-bg/fg), row highlight, shortlist card, and campaign guidance tones (`apps/web/src/styles/tokens.css`)

---

## Foundation (5 tasks)

Core structures and base implementations.

- [x] T003 [S0204] [P] Create pipeline-empty-state.tsx -- loading, empty, error, and offline states with explicit operator-readable messages and token-compliant styling (`apps/web/src/pipeline/pipeline-empty-state.tsx`)
- [x] T004 [S0204] [P] Create pipeline-row.tsx -- dense hybrid-row component showing company, role, score chip, status pill, legitimacy badge, warning count, report/PDF ready indicators, and job posting link in one scannable row with token-compliant styling (`apps/web/src/pipeline/pipeline-row.tsx`)
- [x] T005 [S0204] [P] Create pipeline-filters.tsx -- sticky filter bar with section toggles (all/pending/processed) and sort controls (company/queue/score) plus pagination and count badges, with token-compliant styling (`apps/web/src/pipeline/pipeline-filters.tsx`)
- [x] T006 [S0204] [P] Create pipeline-shortlist.tsx -- shortlist context section with metric cards (last refreshed, generated by, bucket counts), top roles list, and campaign guidance, all token-compliant (`apps/web/src/pipeline/pipeline-shortlist.tsx`)
- [x] T007 [S0204] [P] Create pipeline-context-detail.tsx -- evidence rail detail panel showing selected row metadata (role, company, queue state, score, legitimacy, verification), artifact links, warning review, source line, and report header snapshot with token-compliant styling (`apps/web/src/pipeline/pipeline-context-detail.tsx`)

---

## Implementation (9 tasks)

Main feature implementation.

- [x] T008 [S0204] Rebuild pipeline-review-surface.tsx as a composition of extracted components -- replace the monolithic render with pipeline-empty-state, pipeline-shortlist, pipeline-filters, pipeline-row list, and pipeline-context-detail, removing all inline hex/RGB values (`apps/web/src/pipeline/pipeline-review-surface.tsx`)
- [x] T009 [S0204] Rewrite all user-visible strings in pipeline-review-surface.tsx -- remove "Phase 04 / Session 04" header, replace "pipeline-review payload", "queue-review surface", "shortlist context", and other internal terms with operator-focused terse copy (`apps/web/src/pipeline/pipeline-review-surface.tsx`)
- [x] T010 [S0204] Rewrite empty state messages in getEmptyState -- replace "pipeline-review endpoint", "queue-review payload", "pipeline-review summary" and other internal API terms with operator-readable descriptions (`apps/web/src/pipeline/pipeline-empty-state.tsx`)
- [x] T011 [S0204] Wire pipeline-page.tsx to pass evidence rail content via outlet context -- when a row is selected, provide pipeline-context-detail as evidence rail content following the session 02 outlet context pattern (`apps/web/src/pages/pipeline-page.tsx`)
- [x] T012 [S0204] Add responsive layout behavior to pipeline-review-surface.tsx -- 3-tier layout (mobile stacked, desktop side-by-side with evidence rail, wide with generous spacing) matching session 03 responsive patterns (`apps/web/src/pipeline/pipeline-review-surface.tsx`)
- [x] T013 [S0204] Migrate warning tone colors in pipeline-review-surface.tsx -- replace getWarningTone inline hex values with design token references for low-score, suspicious-legitimacy, caution-legitimacy, missing-pdf, missing-report, and stale-selection (`apps/web/src/pipeline/pipeline-review-surface.tsx`)
- [x] T014 [S0204] Migrate all remaining inline hex/RGB values in pipeline files -- audit pipeline-row.tsx, pipeline-filters.tsx, pipeline-shortlist.tsx, pipeline-context-detail.tsx, and pipeline-empty-state.tsx for any missed raw color values and replace with tokens (`apps/web/src/pipeline/`)
- [x] T015 [S0204] Add refreshing indicator to pipeline surface -- show visual feedback (disable-while-pending, opacity reduction, or spinner) when data is refetching after filter/sort/refresh actions with duplicate-trigger prevention while in-flight (`apps/web/src/pipeline/pipeline-review-surface.tsx`)
- [x] T016 [S0204] Handle stale selection on filter change -- when section or sort changes cause the selected row to disappear, show explicit stale-selection state with clear operator guidance and state reset on re-entry (`apps/web/src/pipeline/pipeline-context-detail.tsx`)

---

## Testing (4 tasks)

Verification and quality assurance.

- [x] T017 [S0204] Run TypeScript compiler and Vite build -- verify zero type errors in strict mode and clean production build with no warnings (`apps/web/`)
- [x] T018 [S0204] Run banned-terms copy check on all pipeline files -- execute `node scripts/check-app-ui-copy.mjs` and verify zero violations in pipeline directory (`apps/web/src/pipeline/`)
- [x] T019 [S0204] Validate ASCII encoding and token compliance on all new and modified files -- verify no non-ASCII characters, no inline hex/RGB color values, and Unix LF line endings (`apps/web/src/pipeline/`)
- [x] T020 [S0204] Manual testing and verification -- test filter/sort/select/paginate flows, verify evidence rail updates without route churn, verify responsive layout at mobile/desktop/wide breakpoints, verify empty/error/offline states render correctly

---

## Completion Checklist

Before marking session complete:

- [x] All tasks marked `[x]`
- [x] All tests passing
- [x] All files ASCII-encoded
- [x] implementation-notes.md updated
- [x] Ready for the validate workflow step

---

## Next Steps

Run the implement workflow step to begin AI-led implementation.
